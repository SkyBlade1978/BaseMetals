# OreSpawn 4.0.16 schemas

This directory is a vendored verification snapshot of the public OreSpawn
4.0.16 schema documents. Base Metals validates its schema-3, revision-1
provider against this exact snapshot during `check`; the runtime dependency
range remains `[4.0.1,5.0.0)`.
