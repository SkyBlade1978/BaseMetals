# OreSpawn 4.0.1 schemas

These files are an unmodified validation snapshot from the OreSpawn 4.0.1
project in the sibling workspace. They are vendored so Base Metals' unit tests
validate the packaged provider against the exact engine version required by
`mods.toml`.
